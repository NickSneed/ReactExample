import TextBlock from './../components/TextBlock';

const Home = () => {
    const home = (
        <>
            <TextBlock copy={'home'} />
        </>
    )
    return home;
};

export default Home;