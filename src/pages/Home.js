import TextBlock from './../components/TextBlock.js';

const Home = () => {
    const home = (
        <>
            <TextBlock copy={'Home'} />
        </>
    )
    return home;
};

export default Home;