import TextBlock from './../components/TextBlock'

const Blogs = () => {
    const page = (
        <>
            <TextBlock copy={'blogs'} />
        </>
    )
    return page;
};

export default Blogs;